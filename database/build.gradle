dependencies {
    compile(
            fileTree(dir: "lib", include: "*.jar"),
            project(":models"),
            runtimeDeps.json,
            runtimeDeps.commons,
            runtimeDeps.jooq
    )
}